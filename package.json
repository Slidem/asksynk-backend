{
  "name": "asksynk-backend",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": "24.13.0",
    "pnpm": "10.15.0"
  },
  "scripts": {
    "dev:api": "pnpm --filter @asksynk/api dev",
    "dev:worker": "pnpm --filter @asksynk/background-worker dev",
    "dev:migrate": "pnpm --filter @asksynk/migrations db:push",
    "dev:generate-migrations": "pnpm --filter @asksynk/migrations db:generate",
    "dev:new-custom-migration": "pnpm --filter @asksynk/migrations db:generate-custom",
    "localdev:up": "bash localdev/up.sh",
    "localdev:down": "bash localdev/down.sh",
    "localdev:reset": "bash localdev/reset.sh",
    "test:integration:db:up": "docker compose -f localdev/docker-compose.test.yml up -d --wait",
    "test:integration:db:down": "docker compose -f localdev/docker-compose.test.yml down",
    "test:integration": "pnpm --filter @asksynk/api test",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier -w .",
    "format:check": "prettier --check .",
    "test": "pnpm -r test",
    "secret:better-auth": "pnpm --filter @asksynk/scripts generate-secret"
  },
  "devDependencies": {
    "@eslint/js": "^9.7.0",
    "eslint": "^9.7.0",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "eslint-plugin-unused-imports": "^4.4.1",
    "prettier": "^3.3.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.20.0"
  }
}
