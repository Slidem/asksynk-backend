{
  "name": "asksynk-backend",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": "24.13.0",
    "pnpm": "10.15.0"
  },
  "scripts": {
    "dev:api": "pnpm --filter @asksynk/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
