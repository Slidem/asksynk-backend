# API service

NestJS API server for core backend operations.

## Endpoints

- `GET /health`: health check

## Local run

1. Install deps: `pnpm install`
2. Start API: `pnpm --filter @asksynk/api dev`

## Notes

- Port: 3000
